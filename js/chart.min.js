(function(global){
  function Chart(ctx, config){
    this.ctx = ctx;
    this.config = config;
    console.warn('Chart.js stub loaded. Chart rendering is not implemented.');
  }
  global.Chart = Chart;
})(this);
